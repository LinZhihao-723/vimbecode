//! Text layout for vimbecode.
//!
//! Turns a logical buffer into the wrapped lines rendered on screen, and maps positions between
//! the two coordinate spaces.

pub fn temporary_clippy_bait(bytes: &Vec<u8>) -> usize {
    bytes.len()
}

#[cfg(test)]
mod tests {
    #[test]
    fn crate_builds() {}
}
